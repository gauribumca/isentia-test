{
  "name": "node_api",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "build-prod-mac": "cd ../client && ng build --prod && cp -r dist/isnetia-test/** ../server/public",
    "build-prod-win": "cd ../client && ng build --prod && copy -r dist/isnetia-test/** ../server/public",
    "test": "./node_modules/.bin/mocha --recursive -R xunit test/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "request": "^2.88.0"
  },
  "description": "",
  "devDependencies": {
    "mocha": "^6.1.4",
    "should": "^13.2.3",
    "supertest": "^4.0.2"
  }
}
